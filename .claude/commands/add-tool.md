Add a new tool's config to the dotfiles repo.

1. Identify the tool's config file location
2. Copy it into a new directory under this repo
3. Add a symlink command to install.sh
4. Update README.md with the new tool
5. Update CLAUDE.md structure section

Tool name: $ARGUMENTS
